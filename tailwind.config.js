/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./**/*.{html,js}"], // Ensure it includes all subdirectories
  darkMode: false, // 'media' or 'class' based on preference
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
